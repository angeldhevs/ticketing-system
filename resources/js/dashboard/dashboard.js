/**
 * Dashboard Layout (via Gridstack)
 */

$(function() {
  //Initialize module default options.
  //Cache jQuery DOM references.
  $root = $('#dashboard');

  function initialize() {
    render();
  }

  function render() {
  }

  initialize();
});